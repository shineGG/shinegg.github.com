参与项目（visual studio code 、webstrom 、NetBeans）基础 HTML css js
1、测试v5.5ikeshang.com教学互动平台：在线视频剪辑功能、在语音课时功能（版本更新）
2、修改新增人员页面布局
(接触portal端，使用SVN更新代码后使用shell命令更新服务器sudo su  up.sh)
3、设备管理页面项目：
a、开发设备管理中教室使用统计页面；（使用vue-cli webpack elementUI echarts）
b、使用VUE elementUI开发单页应用时，<style scoped></style>标签里面要增加一个scoped属性，这样在某个页面中的样式不会污染全局样式，而对elementUI组件样式进行修改时不能加scoped属性限制否则无法设置定制样式。但是在控制台中拿到的elementUI组件的class名虽然可以直接使用，但是由于没有scoped限制，直接使用element的class修改element组件的样式绘导致其他页面使用相同element组件时样式冲突，因此使用element组件class名，在其前面加祖先元素的选择器进行限定。此次项目就出现样式冲突情况，导致从设置页面跳转至统计页面时需要手动刷新统计页面，这样统计页面布局才不乱。一些导至全局冲突的样式不好找出。也要注意选择器优先级问题。
学习技能
1、原生微信小程序开发
2、基于mpvue的微信小程序开发
3、实现微信小程序涂鸦画板demo
参与代码评审
1、组长、项目经理带着评审每个文件修改的代码
